#ifndef NEOPIXEL_H    //include guards to avoid double inclusion of library   
#define NEOPIXEL_H    //include guards to avoid double inclusion of library

void neopixel_init();
void neopixel_set(uint32_t rgb);

#endif